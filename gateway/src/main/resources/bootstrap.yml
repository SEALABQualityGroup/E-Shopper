spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      retry:
        max-attempts: 300
        max-interval: 1000
      password: password
      username: user
  session:
    store-type: none
